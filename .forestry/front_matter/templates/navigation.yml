---
label: Navigation
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: navigation
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: pages
  label: Navigation
pages:
- content/global/primary-navigation.md
