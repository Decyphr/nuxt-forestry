---
label: Navigation
hide_body: true
fields:
- name: navigation
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: articles
  label: Navigation
