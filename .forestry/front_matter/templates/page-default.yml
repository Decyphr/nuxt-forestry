---
label: Page Default
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: Title of this specific page.
- name: featured_image
  type: file
  config:
    maxSize: 3
  label: Featured Image
  description: Featured image to be used as an identifier for the page.
- name: excerpt
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Excerpt
  description: Short description of the page
- name: content_blocks
  type: blocks
  template_types:
  - accordion-block
  - blockquote-block
  config:
    min: 
    max: 
  label: Content Blocks
  description: Content building blocks for the page.
